# 이 저장소의 작업 규칙

개인 웹사이트 한 개를 만드는 저장소다. 정적 사이트이며 빌드 단계가 없다.
Cloudflare Pages가 저장소 최상위를 그대로 서빙한다.

사용자의 명시적 지시가 이 규칙보다 우선한다. 다만 **지시가 규칙과 충돌하면
어떤 규칙과 충돌하는지 한 줄로 알리고 나서 지시를 따른다.** 조용히 어기지 않는다.

---

## 이 사이트의 목적

- 누가 보는가: UNIST 동료 학생·교수
- 무엇을 하길 바라는가: 박민성을 소개 (산업공학과 3학년, 금융 AI 관심, DIL Lab 활동)
- 이 사이트가 다른 사람 것으로 바뀌면 성립하지 않는 문장: "산업공학과 3학년으로 금융 AI, 자산관리, 투자에 관심이 있고 DIL Lab에서 활동한다"
- 소재에서 빌려올 시각 언어: (아직 없음 — sources/ 가 비어 있어 기본값 팔레트로 시작. 실제 소재가 생기면 이 항목과 색 변수를 갱신)

**위 항목 중 하나라도 `(미기입)`이면 어떤 파일도 수정하기 전에 먼저 묻는다.**

---

## 파일 구조

```
index.html   단일 페이지. 이 파일이 사이트다.
style.css    모든 스타일.
script.js    없어도 사이트가 동작해야 한다. 만들 때는 이유를 먼저 말한다.
images/      이미지. 폭 1600px 이하로 줄이고 width/height 속성을 명시한다.
fonts/       셀프호스팅 woff2.
sources/     출처 사진·스캔. 아래 출처 규칙 참조.
AGENTS.md    이 파일.
```

이 여섯 개 외에 새 파일이나 폴더를 만들지 않는다.
프레임워크, 빌드 도구, 패키지 매니저, CSS 전처리기를 도입하지 않는다.
`package.json`을 만들지 않는다.

**외부 JavaScript를 불러오지 않는다.** 폰트는 `fonts/`에 셀프호스팅한다.
CDN `<link>`로 폰트를 불러오지 않는다.

## 수정 방식

**전체를 다시 생성하지 않는다.** 요청이 한 부분에 관한 것이면 그 부분만 고친다.
사용자가 "처음부터 다시"라고 명시할 때만 전면 재작성한다.

- 고치기 전에 해당 파일의 현재 내용을 읽는다.
- 무엇을 왜 바꿨는지 두 문장 안에 설명한다.
- 요청하지 않은 부분을 "개선"하지 않는다. 눈에 띄는 문제가 있으면
  고치지 말고 말로 알린다.

**에이전트는 커밋하거나 푸시하지 않는다.** 파일을 변경하고 멈춘다.
커밋과 푸시는 사용자가 변경 내용을 직접 읽은 뒤 한다.
`git push --force`를 실행하지 않는다.

작업을 마치면 아래 형식으로 보고한다.

```
바꾼 파일: (목록)
확인한 것: (아래 "기준선" 중 확인한 항목)
확인 못 한 것: (브라우저가 없어 확인 불가한 항목)
```

확인 못 한 것을 확인했다고 말하지 않는다.

---

## 출처 규칙

이 저장소의 핵심 규칙이다.

**기본값이 아닌 선택에는 출처 주석을 단다.** 특이한 서체, 특이한 레이아웃,
특이한 색, 특이한 장식을 넣을 때는 어디서 왔는지 한 줄로 적는다.

```css
/* 출처: sources/library-card.jpg 의 날짜 스탬프 칸.
   글 목록의 날짜를 같은 리듬으로 정렬한다. */
.post-date { ... }
```

**출처는 사용자가 준다. 에이전트는 출처를 만들어내지 않는다.**
사용자가 말하지 않은 소재를 "~를 연상시킨다", "~에서 착안했다"고 적는 것은
출처가 아니라 지어낸 것이다.

**출처 주석은 `sources/` 폴더의 실제 파일명을 가리켜야 한다.**
해당 파일이 없으면 그 장치를 넣지 말고 사용자에게 소재를 묻는다.

판별 기준: **그 출처가 없었어도 이 장치를 넣었을 것 같으면 기본값이다.**

출처는 웹 밖이 낫다. 도면, 악보, 영수증, 표지판, 시간표, 지도 범례,
전시 도록, 게임 UI, 관공서 서식, 스포츠 기록지, 약 봉투, 카세트 라벨.
다른 웹사이트를 참고하면 그 웹사이트를 닮는다.

### 색

**색은 화면에서 고르지 않는다.** `sources/`의 사진에서 스포이드로 집는다.
사용자가 hex 값을 직접 주지 않았다면, 어느 사진의 어느 부분에서 뽑을지 묻는다.

색·간격·글자 크기의 정의 방식은 `style.css` 맨 위 변수 블록을 따른다.
그 블록에 없는 값을 새로 발명하지 않는다.

---

## 기본값 목록

아래는 생성 모델이 요청 없이 넣는 기본값이다. 기본적으로 사용하지 않는다.

**색**
- 보라·남색 계열 그라데이션 배경
- 텍스트에 적용된 그라데이션
- 장식 목적의 blur/glow 원형 요소
- 요청하지 않은 다크 모드 (검정에 가까운 배경 + 형광 강조색)

**레이아웃**
- 화면 높이를 채우는 중앙 정렬 hero
- 3열 카드 그리드
- 모든 요소에 동일한 border-radius
- 모든 카드에 동일한 회색 그림자
- 카드 안의 카드
- glassmorphism, 반투명 패널

**타이포그래피**
- 시스템 폰트 스택만 쓰고 끝내기
- 제목에서 한 단어만 색이나 굵기로 강조하기
- 라벨을 대문자로 자간 벌려 쓰기
- 제목 위에 정체불명의 라벨 한 줄 추가하기

**구조**
- 요청하지 않은 섹션 추가 (기능 소개, 통계, 후기, FAQ, 뉴스레터, CTA 배너)
- 가짜 숫자, 가짜 후기, 가짜 로고
- 내용이 시퀀스가 아닌데 01 / 02 / 03 번호 붙이기
- 의미 없는 구분선과 라벨

**모션**
- 섹션마다 fade-in / slide-up
- 모든 카드에 hover 확대·그림자 변화
- 사용자 동작에 대한 응답이 아닌 애니메이션

**카피**
- 이름만 바꾸면 다른 사람 사이트가 되는 문장
- 링크나 버튼 텍스트 끝의 화살표 기호
- 근거 없는 최상급 표현

### 목록의 장치를 쓰려면

이건 금지가 아니라 **기본값 목록**이다. 이 사이트에 맞다면 쓸 수 있다.
조건은 위 출처 규칙과 같다. `sources/`의 실제 파일을 가리키는 출처 주석을
달고, 왜 여기서는 기본값이 아니라 선택인지 적는다.

```css
/* 출처: sources/floor-plan.jpg 의 메타 표기 (IL-04 · 거실 · 34평 · 2025).
   기본값 목록의 "대문자 자간 벌림"에 해당하지만,
   이 사이트의 소재가 도면이므로 표기법을 빌려온 것이다. */
.spec { text-transform: uppercase; letter-spacing: 0.14em; }
```

### 관습 깨기

**사용자가 명시적으로 요청할 때만** 웹의 일반 관습을 어긴다.
스크롤 방향, 내비게이션의 부재, 의도적인 지연 같은 것. 에이전트가 먼저 제안하지 않는다.

사이트 전체에서 한 가지만 어긴다. 그리고 아래 기준선은 예외 없이 지킨다.
키보드로 도달할 수 없거나 스크린리더가 읽을 수 없는 것은 파격이 아니라 고장이다.

---

## 대신 할 것

- **폰트는 의도적으로 고른다.** 한 종류, 많아야 두 종류. 두 종류면 확실히 달라야 한다.
  교체 지점은 `--font-body` / `--font-display` 두 변수뿐이다. 다른 곳에 폰트 이름을 적지 않는다.
- **크기 위계를 만든다.** 제목·본문·캡션의 크기 차이가 중요도를 설명해야 한다.
- **여백에 리듬을 준다.** 모든 섹션의 상하 여백이 같으면 위계가 사라진다.
- **강조는 한 곳에만 한다.** 기억에 남을 요소 하나를 정하고 나머지는 조용히 둔다.

## 한국어 조판

`style.css`에 이미 반영되어 있다. 지우거나 덮어쓰지 않는다.

`lang="ko"` / `word-break: keep-all` + `overflow-wrap: break-word` /
본문 자간 0~-0.02em, 제목은 좁히지 않음 / 행간 본문 1.6~1.8, 제목 1.2~1.4 /
본문 16px 이상 / 한 줄 45자 이하 (`max-width`는 em으로. `ch`는 한글에 맞지 않는다) /
폰트 스택은 라틴 서체를 먼저, 한글 서체를 뒤에.

## 코드 규칙

- semantic HTML을 쓴다. `<div>`로 모든 것을 만들지 않는다.
- 선택자 특이도를 낮게 유지한다. `!important`를 쓰지 않는다.
- 문제가 생겼을 때 `overflow: hidden`으로 덮지 않는다. 원인을 고친다.

## 기준선

장식보다 먼저 성립해야 한다.

1. `<html lang="ko">`가 있다.
2. 모바일 폭(360px)에서 가로 스크롤이 생기지 않는다.
3. 키보드 Tab으로 모든 링크·버튼에 도달할 수 있고 포커스가 보인다.
4. 이미지에 alt 텍스트가 있다. 장식용이면 `alt=""`.
5. 본문과 배경의 명도 대비가 충분하다.
6. `prefers-reduced-motion`을 존중한다.
7. 작동하지 않는 링크나 버튼을 만들지 않는다. 아직 없으면 넣지 않는다.

## 절대 금지

- API 키, 토큰, 비밀번호를 어떤 파일에도 쓰지 않는다.
- 브라우저 코드에서 AI API를 직접 호출하지 않는다.
- 실제 개인정보(전화번호, 주소)를 예시로 넣지 않는다.
- 확인받지 않고 파일을 삭제하지 않는다.

---

## 선택 트랙 예외

아래는 **사용자가 명시적으로 해당 트랙을 선택했다고 말한 경우에만** 적용된다.
그 말이 없으면 위 규칙이 그대로 우선한다.

### Sveltia CMS 트랙

글을 자주 쓰는 사용자만 선택한다. 이 트랙에서는 아래가 허용된다.

- `admin/index.html`과 `admin/config.yml` 추가
- Sveltia CMS 스크립트를 CDN에서 불러오는 것 (`admin/` 안에서만)
- `content/` 폴더와 그 안의 JSON 파일
- `script.js`에서 `content/`의 JSON을 fetch해 목록을 렌더링하는 것

허용되지 않는 것은 그대로 유지된다. 정적 사이트 생성기를 도입하지 않고,
마크다운 파서를 넣지 않고, 빌드 단계를 만들지 않는다.
콘텐츠 형식은 JSON으로 고정한다.

`admin/`은 사이트의 일부가 아니다. 위 기준선과 기본값 목록은
`index.html`과 `style.css`에만 적용한다.
